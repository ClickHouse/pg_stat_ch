name: Setup vcpkg
description: Install vcpkg, set VCPKG_ROOT, and configure binary caching

inputs:
  triplet:
    description: "vcpkg triplet (used for cache key)"
    required: false
    default: "x64-linux-pic"

runs:
  using: composite
  steps:
    - name: Install vcpkg
      shell: bash
      env:
        # Pin to the same commit as the vcpkg submodule and the Dockerfile
        # (vcpkg tag 2026.04.27).  Without this checkout the action lands on
        # master HEAD, which drifts port-version ABI hashes and silently
        # invalidates the binary archive cache between CI runs.
        VCPKG_COMMIT: 56bb2411609227288b70117ead2c47585ba07713
      run: |
        if [ ! -d "$HOME/vcpkg" ]; then
          # Full clone required — Arrow depends on Boost, which needs vcpkg git history
          git clone https://github.com/microsoft/vcpkg.git "$HOME/vcpkg"
          git -C "$HOME/vcpkg" checkout "$VCPKG_COMMIT"
          "$HOME/vcpkg/bootstrap-vcpkg.sh" -disableMetrics
        else
          git -C "$HOME/vcpkg" fetch --quiet origin "$VCPKG_COMMIT" || true
          git -C "$HOME/vcpkg" checkout "$VCPKG_COMMIT"
        fi
        echo "VCPKG_ROOT=$HOME/vcpkg" >> "$GITHUB_ENV"
        echo "$HOME/vcpkg" >> "$GITHUB_PATH"

        # Use a stable directory for vcpkg binary cache (runner.temp can be wiped)
        mkdir -p "$HOME/.cache/vcpkg-archives"
        echo "VCPKG_DEFAULT_BINARY_CACHE=$HOME/.cache/vcpkg-archives" >> "$GITHUB_ENV"

    - name: Cache vcpkg binary archives
      uses: actions/cache@v4
      with:
        path: ~/.cache/vcpkg-archives
        key: vcpkg-${{ inputs.triplet }}-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'triplets/*.cmake', 'overlay-ports/**') }}
        restore-keys: |
          vcpkg-${{ inputs.triplet }}-
