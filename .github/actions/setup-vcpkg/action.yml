name: Setup vcpkg
description: Install vcpkg, set VCPKG_ROOT, and configure binary caching

inputs:
  triplet:
    description: "vcpkg triplet (used for cache key)"
    required: false
    default: "x64-linux-pic"

runs:
  using: composite
  steps:
    - name: Install vcpkg
      shell: bash
      run: |
        if [ ! -d "$HOME/vcpkg" ]; then
          git clone --depth 1 https://github.com/microsoft/vcpkg.git "$HOME/vcpkg"
          "$HOME/vcpkg/bootstrap-vcpkg.sh" -disableMetrics
        fi
        echo "VCPKG_ROOT=$HOME/vcpkg" >> "$GITHUB_ENV"
        echo "$HOME/vcpkg" >> "$GITHUB_PATH"

        # Use a fixed directory for vcpkg binary cache
        mkdir -p "${{ runner.temp }}/vcpkg-archives"
        echo "VCPKG_DEFAULT_BINARY_CACHE=${{ runner.temp }}/vcpkg-archives" >> "$GITHUB_ENV"

    - name: Cache vcpkg binary archives
      uses: actions/cache@v4
      with:
        path: ${{ runner.temp }}/vcpkg-archives
        key: vcpkg-${{ inputs.triplet }}-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'triplets/*.cmake') }}
